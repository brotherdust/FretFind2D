# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# IDE / Editor specific
# .vscode/ # FretFind2D.code-workspace is tracked, so .vscode might be shared or not used for settings that should be ignored.
# Add other editor/IDE specific files if used, e.g. .idea/ for JetBrains

# Application Output Files
# These are generated by FretFind2D. If saved in the project directory during development/testing, they should be ignored.
*.pdf
*.svg
*.dxf
*.csv
*.tab

# HTML files - ignore all, then explicitly un-ignore those that are part of the project
*.html
!src/fretfind.html
!test/geom.html
!test/pdf.html

# Python cache files (for scripts in references/fromInkscape/)
__pycache__/
*.pyc
*.pyo
*.pyd

# Docker-related (if any local files are generated that aren't part of the committed docker setup)
# e.g. docker/logs/ if logs are stored locally and not meant to be committed

# General logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temporary files
*.tmp
*.temp
*~
